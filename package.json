{
  "name": "dsh-dev-rules",
  "version": "26.9.21-1",
  "description": "Felix 项目开发规则：DSH 个人插件。在 Web GUI 右侧栏提供「开发规则」面板（与「Docker 容器」并列，全局规则 + 按项目覆盖），并把当前项目生效的规则自动注入每个会话的系统提示，让 agent 开发项目时自动遵守；另含 dev_rules 工具，便于在对话里直接维护规则。",
  "keywords": [
    "deepseek",
    "deepseek-harness",
    "dsh",
    "dsh-plugin",
    "rules",
    "dev-rules",
    "prompt",
    "web"
  ],
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./client": "./lib/client.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md",
    "NOTICE.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Grant-Felix/dev-rules.git"
  },
  "homepage": "https://github.com/Grant-Felix/dev-rules#readme",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check lib/index.js && node --check lib/rules.js && node --check lib/client.js && node --test",
    "sandbox": "bash scripts/sandbox.sh boot",
    "sandbox:check": "bash scripts/sandbox.sh check",
    "sandbox:clean": "bash scripts/sandbox.sh clean"
  },
  "license": "MIT"
}
